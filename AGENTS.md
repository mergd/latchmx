# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Do not run `eas build` or EAS Submit. Prefer a local iOS build here (Xcode / `npx expo run:ios`).
